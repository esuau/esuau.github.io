language: node_js
node_js:
  - "11"

branches:
  except:
  - master

script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: dist/evan-suau/
  target-branch: master
  on:
    branch: release
